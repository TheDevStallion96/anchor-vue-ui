# Docker API Configuration
# The host where the Docker API backend is running
VITE_DOCKER_API_HOST=localhost

# The port where the Docker API backend is running
VITE_DOCKER_API_PORT=3005

# Complete base URL for the Docker API (automatically constructed if not provided)
VITE_DOCKER_API_BASE_URL=http://localhost:3000/api/v1